services:
  - type: web
    name: nova-dashboard-api
    env: node
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/auth/me
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: nova2026
      - key: PORT
        value: "3002"
